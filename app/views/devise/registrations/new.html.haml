.header
  = link_to image_tag("ToripLog.png", class: "Trip_Log_Logo")
.AccountPage
  .AccountPage__title
    %h2 無料会員登録
    %h5 会員ではない方
    = render "devise/shared/links"
  .AccountPage__form
    = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
      = devise_error_messages!
      .FormField
        .FormField__label--normal
          = f.label :Name
        .FormField__input--normal
          = f.text_field :name, autofocus: true
      .FormField
        .FormField__label--normal
          = f.label :email
        .FormField__input--normal
          = f.email_field :email
      .FormField
        .FormField__label--normal
          = f.label :パスワード
          %i (英数字6文字以上)
        .FormField__input--normal
          = f.password_field :password, autocomplete: "off"
      .FormField
        .FormField__label--normal
          = f.label :パスワード 
          %i (確認用 再入力)
        .FormField__input--normal
          = f.password_field :password_confirmation, autocomplete: "off"
      .actions
        = f.submit "会員登録", class: 'Button UserButton'

.AccountPage__Second
  .AccountPage__Second__Contents
    Trip Logでは様々なサービスがあります。
  .AccountPage__Second__ContentRight
    .AccountPage__Second__ContentRight__One
      %i.fas.fa-users
      %h1 コミュニティグループ
      %p 旅の話題で盛り上がる
    .AccountPage__Second__ContentRight__Two
      %i.fas.fa-gifts
      %h1 フリーマーケット
      %p 旅の必需品を手に入れよう
    .AccountPage__Second__ContentRight__Three
      %i.fas.fa-book
      %h1 ブログ
      %p 旅の記録を積み重ねよう

.AccountPage__Third
  .AccountPage__Third__Contents
    さらにこだわりを持つあなたに
    .AccountPage__Third__Contents__One

.AccountPage__Fourth
  .AccountPage__Fourth__Contents
    あなたと一緒に成長する仲間がいる
    .AccountPage__Fourth__Contents__One

.AccountPage__Fifth
  .AccountPage__Fifth__Contents
    あなたも始めて見ませんか？
    .AccountPage__Fifth__Contents__One



#page_top
  %a{:href => "#"}
